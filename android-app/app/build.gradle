plugins {
    id 'com.android.application'
}

android {
    namespace 'com.aiupworkagent.mobile'
    compileSdk 35

    defaultConfig {
        applicationId 'com.aiupworkagent.mobile'
        minSdk 23
        targetSdk 35
        versionCode 3
        versionName '1.1.1'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
